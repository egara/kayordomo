{
  # Kayordomo configuration file
  # ----------------------------

  # Server IP address where this server is going to be executed. Typically will be 0.0.0.0
  server_ip: 0.0.0.0, 
  # Server port where this service is going to be exposed
  server_port: 8083,
  # Kodi IP address where Kodi is executed
  kodi_ip: localhost,
  # Kodi port where JSONRPC is available
  kodi_port: 8080
}
